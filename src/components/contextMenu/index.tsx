import ContextMenuWrapper from "./wrapper";
import ContextMenu, { ContextMenuEntry } from "./contextmenu";

export type Entry = ContextMenuEntry;
export const Menu = ContextMenu;
export const Wrapper = ContextMenuWrapper;
